{
  "name": "marica",
  "private": false,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "storybook": "storybook dev -p 6006 --config-dir packages/marica-components/.storybook",
    "build-storybook": "storybook build --config-dir packages/marica-components/.storybook",
    "chromatic": "chromatic --exit-zero-on-changes"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
