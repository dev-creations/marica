{
  "name": "marica",
  "private": false,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "storybook": "storybook dev -p 6006 --config-dir packages/marica-react/.storybook",
    "build-storybook": "storybook build --config-dir packages/marica-react/.storybook",
    "chromatic": "chromatic --exit-zero-on-changes"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "dependencies": {
    "iconoir": "^6.5.0"
  }
}
